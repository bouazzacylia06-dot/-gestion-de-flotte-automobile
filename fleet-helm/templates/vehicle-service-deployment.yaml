{{- if .Values.vehicleService.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehicle-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fleet.serviceLabels" . | nindent 4 }}
    app.kubernetes.io/name: vehicle-service
    app.kubernetes.io/component: microservice
spec:
  replicas: {{ .Values.vehicleService.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: vehicle-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vehicle-service
        app.kubernetes.io/component: microservice
        app.kubernetes.io/part-of: fleet-management
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.vehicleService.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      {{- include "fleet.podSecurityContext" . | nindent 6 }}
      serviceAccountName: {{ include "fleet.serviceAccountName" . }}
      containers:
        - name: vehicle-service
          image: "{{ .Values.vehicleService.image.repository }}:{{ .Values.vehicleService.image.tag }}"
          imagePullPolicy: {{ .Values.vehicleService.image.pullPolicy | default .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.vehicleService.port }}
              protocol: TCP
          {{- include "fleet.securityContext" . | nindent 10 }}
          envFrom:
            - configMapRef:
                name: {{ include "fleet.configmapName" . }}
          env:
            - name: OTEL_SERVICE_NAME
              value: "vehicle-service"
            - name: PORT
              value: {{ .Values.vehicleService.port | quote }}
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "fleet.postgresSecretName" . }}
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "fleet.postgresSecretName" . }}
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: {{ .Values.vehicleService.resources.requests.cpu | quote }}
              memory: {{ .Values.vehicleService.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.vehicleService.resources.limits.cpu | quote }}
              memory: {{ .Values.vehicleService.resources.limits.memory | quote }}
          readinessProbe:
            httpGet:
              path: {{ .Values.vehicleService.readinessProbe.path }}
              port: http
            initialDelaySeconds: {{ .Values.vehicleService.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.vehicleService.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.vehicleService.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.vehicleService.readinessProbe.failureThreshold }}
          livenessProbe:
            httpGet:
              path: {{ .Values.vehicleService.livenessProbe.path }}
              port: http
            initialDelaySeconds: {{ .Values.vehicleService.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.vehicleService.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.vehicleService.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.vehicleService.livenessProbe.failureThreshold }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
{{- end }}
