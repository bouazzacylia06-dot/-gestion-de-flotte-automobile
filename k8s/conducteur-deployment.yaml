apiVersion: apps/v1
kind: Deployment
metadata:
  name: conducteur-service
  namespace: conducteur-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: conducteur-service
      version: v1.0.0
  template:
    metadata:
      labels:
        app: conducteur-service
        version: v1.0.0
    spec:
      containers:
        - name: conducteur-service
          image: conducteur-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
          env:
            - name: APP_PORT
              value: "3001"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: url
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
